# Synced from Backblaze at build time — never committed.
/content/*.jpg
/content/*.jpeg
/content/*.JPG
/content/*.JPEG

# Hugo
/public/
/resources/
/.hugo_build.lock

# Local credentials for `just sync`
/.env

# Staged HEIC originals and the summaries written by `tools/b2 pull` / `zip`
/content/.originals/
/data/album.json
/data/originals.json

# The four HEIC originals, kept until the JPEGs in the bucket are trusted
/.heic-backup/
